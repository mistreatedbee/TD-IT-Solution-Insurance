# CTO Status — Release Gate A / INC-001

**Date:** 2026-08-28 · **From:** engineering (acting on sprint plan + INC-001 action list)  
**Audience:** `cto`, `technical-project-manager`, `cybersecurity-architect`  
**Not a scope decision** — status only.

---

## Executive summary

INC-001 Path A engineering work for criterion 6 is **code-complete on `main`**. Build #2 remains **blocked** on human gates: A-13 bundle verification (`security-engineer`), criterion 6 chair sign-off (`cybersecurity-architect`), and owner items 1.1/1.2 (signing identity, Resend delivery).

---

## INC-001 action register (engineering-owned)

| ID | Status | Evidence on `main` |
|---|---|---|
| A-12 | **Done** | `FEATURE_*` flags + layout/route guards; `eas.json` preview/production all `"false"` |
| A-15 | **Done** | `mobile/src/query/persistPolicy.ts`; `shouldDehydrateQuery` wired in root layout |
| A-16 | **Done** | `features.ts` comment corrected |
| A-17 | **Done (interim)** | `mobile/app.config.ts` strips `expo-location` plugin + OS permissions when location flag is off at build time |
| CI-1 | **Done** | `docs/organization/gates/stage8-manifest.json` + `npm run verify:stage8-manifest` (passes) |
| CI-2 (partial) | **Done** | `scripts/check-adr-prohibitions.mjs` — eas.json flag + kill-switch checks |
| A-13 | **Ready for owner** | Checklist: `docs/organization/incidents/INC-001-A13-criterion-6-bundle-verification.md` — needs EAS preview APK |
| A-9 | **Blocked** | `backend/scripts/inc-001-location-inventory.ts` ready; requires live `MONGODB_URI` + `DATABASE_URL` |
| A-14 | **Not started** | `008/field-sensitivity-review.md` — multi-role, blocks re-enabling ingestion only |

---

## Release Gate A criteria (memo §2 + criterion 6)

| # | Criterion | Status |
|---|---|---|
| 1 | Signing identity / bundle ID | **Owner** — item 1.1 |
| 2 | Resend email delivery confirmed | **Owner** — item 1.2; pacing risk |
| 3 | Manual QA on device | **Blocked** on 1.1 + 1.2 |
| 4 | Claims build-flag | **Done** (prior sprint) |
| 5 | Release notes | **Pending** 2.1 |
| 6 | No ungated Stage-8-bypass surface in client build | **Engineering done; sign-off withheld** — A-13 + chair |

**3.1 / 3.2 (Gate A close, build #2 ship): RED** until criterion 6 signed and 1.1–1.2 land.

---

## Immediate next actions (by role — no re-sequencing)

1. **`devops-engineer` / owner:** Trigger EAS `preview` build from `main` commit `a0c9243` or later for A-13.
2. **`security-engineer`:** Execute A-13 checklist against that APK; file results in incident folder.
3. **`cybersecurity-architect`:** Criterion 6 sign-off only if A-13 passes; else record dissent per INC-001 §9.4.
4. **`database-architect` + owner credentials:** Run `cd backend && npm run inc-001:location-inventory`; hand output to `compliance-specialist` for A-9.
5. **`technical-project-manager`:** Daily track 1.2 (Resend) and 1.1 (signing identity) — still the pacing risk for Gate A criteria 1–3.

---

## CI additions landed (mechanical verification — INC-001 root-cause remedy)

- `npm run verify:stage8-manifest` — route/screen inventory vs Stage 8 manifest
- `scripts/check-adr-prohibitions.mjs` — Gate A client flag + location kill-switch assertions

Wire both into `.github/workflows/ci.yml` on next commit.

---

## Risk accepted / not accepted

- **Not accepted:** Marking criterion 6 green without A-13 bundle evidence (INC-001 §9.4).
- **Accepted for build #2 internal APK:** Feature 009 protection-centre home shell remains visible with gated entry points removed; chair may still flag in A-13 — home row is waived in stage8 manifest with A-13 verification note.
